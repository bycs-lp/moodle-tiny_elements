{
    "name": "elements",
    "private": true,
    "description": "A plugin for the Moodle TinyMCE editor providing a set of visual components designed explicitly for Learning, based on the parent project componentsforlearning.org.",
    "scripts": {
        "compile-sass": "sass --no-source-map scss/main.scss styles.css; sass --no-source-map scss/editor.scss editor_styles.css"
    },
    "devDependencies": {
        "sass": "^1.103.1"
    }
}
